// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("htmltox", "for htmltox support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("htmltox", "enable htmltox support", "no");

if (PHP_HTMLTOX != "no") {
	EXTENSION("htmltox", "htmltox.c");
}

